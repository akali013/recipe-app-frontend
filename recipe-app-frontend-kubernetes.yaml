# Source: https://docs.docker.com/guides/angular/deploy/

apiVersion: apps/v1
kind: Deployment
metadata:
  name: recipe-app-frontend
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: recipe-app-frontend
  template:
    metadata:
      labels:
        app: recipe-app-frontend
    spec:
      containers:
        - name: angular-container
          image: akali013/recipe-app-frontend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          resources:
            limits:
              cpu: "500m"
              memory: "256Mi"
            requests:
              cpu: "250m"
              memory: "128Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: recipe-app-frontend-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: recipe-app-frontend
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30001